// =============================================================
//  DeepDive — Environment Template  (env.example.js)
//  ✅ SAFE TO COMMIT — placeholder values only
//
//  After cloning:
//    1. Copy this file:  env.example.js → env.js
//    2. Replace every placeholder below with your real value
//    3. Save env.js  (it is gitignored — never committed)
//
//  Where to find Supabase values:
//    Dashboard → Settings → API
// =============================================================

window.__env = {
  // Supabase project URL  (Dashboard → Settings → API → Project URL)
  SUPABASE_URL:  'https://your-project-ref.supabase.co',

  // Anon / Publishable key  (Dashboard → Settings → API → anon public)
  SUPABASE_ANON: 'your-anon-key-here',

  // Admin account email (the address flagged as admin in Supabase Auth)
  ADMIN_EMAIL:   'your-admin@example.com',
};
